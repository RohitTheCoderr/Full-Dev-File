// let keyword
// we can use let variable for declaring a variable name in the place of var like
  
// let firstName = "Rohit";
// console.log(firstName);

// difference between var and let 

// var firstName = "sohan";   // here we are declair variable firstName and assian a value "sohan" by using var keyword
//  firstName  = " suchit";    // but here we want change a value in same variable firstName is "suchit" withough any keyword 
//                             // (because firstName is already define var keyword) but we can again use var keyword no any error like below
//  console.log(firstName);

//  var secondName = "Aryan";  
//   var secondName = "Anshu";     // no nay problem by using again var keyword but some cases we don't know same variable name is already declaired and we not want to change a value 
//                                 // then we are falled and in this case so How we can remove this problem ?
//   console.log(secondtName);       // that is only one soluation using let keyword for declaring a variable name like
             
//  let keyword

let lastName ="thakur";
 console.log(lastName);
  lastName ="kumar";     // we want to change a value then we use withought keyword in let case (let keyword is already declared same variavle name) like this 
  console.log(lastName);
   
//   let midName = "sharma";
//    console.log(midName);

// // let midName = "sharmaraj";     // Error show  Uncaught SyntaxError: Identifier 'midName' has already been declared
//                                   //  It is helpful for we namt want to chnage value but we also use a same name of variable like this
//     console.log(midName);        // if we want change value in same variable then we can change withought using extra let keyword

// converetion
//  let keyword use karna good practice
